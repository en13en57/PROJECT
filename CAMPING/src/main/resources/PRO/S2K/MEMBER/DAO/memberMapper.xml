<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PRO.S2K.CAMP.DAO.MemberDAO">

	<!-- 1. 저장하기 -->
	<insert id="insert" parameterType="MemberVO">
		insert into camp_member
		(mb_ID,mb_password,mb_name,mb_nick,mb_email,mb_tel,mb_birth,mb_zipcode,mb_address1,mb_address2,gr_grade,authkey)
		<!-- insert into camp_member로 그냥 해놓으면 전체적인 값을 집어넣어줘야함 
			위에 같이 이렇게 명시해서 넣으면 필요한 값만 넣음.
		 -->
		 values
		(
		<!-- #{mb_idx}, -->
			#{mb_ID},
			#{mb_password},
			#{mb_name},
			#{mb_nick},
			#{mb_email},
			#{mb_tel},
			#{mb_birth},
			#{mb_zipcode},
			#{address1},
			#{address2},
			'0', <!-- 등급 -->
			'1'  <!-- 임시 authKey 나중에 바꿔야함. -->
			
		)
	</insert>

	<!-- 2. 1개 얻기 -->
	<select id="selectByIdx" parameterType="int" resultType="MemberVO">
		select * from camp_member where mb_idx=#{mb_idx}
	</select>
	<!-- 3. 수정하기 -->
	<update id="update" parameterType="MemberVO">
		update member set
			mb_name = #{mb_name},
			mb_nick = #{mb_nick},
			mb_tel = #{mb_tel},
			mb_zipcode = #{mb_zipcode},
			address1 = #{address1},
			address2 = #{address2},
		where
			mb_idx=#{mb_idx}
	</update>
	<!-- 4. 삭제하기 -->
	<delete id="delete" parameterType="int">
		delete from camp_member where mb_idx=#{mb_idx}
	</delete>
	<!-- 5. 모두보기(관리자만 사용) -->
	<select id="selectList" resultType="MemberVO">
		select * from camp_member order by mb_idx desc
	</select>
	<!-- 6. 개수세기 -->
	<select id="selectCount" resultType="int">
		select count(*) from camp_member
	</select>
	<!-- 7. 해당아이디의 개수세기(아이디 중복확인 : 0이면 없는아이디, 1이상이면 존재하는 아이디) -->
	<select id="selectCountByUserId" resultType="int" parameterType="string">
		select count(*) from camp_member where mb_ID=#{mb_ID}
	</select>
		<!-- 8. 해당아이디의 개수세기(아이디 중복확인 : 0이면 없는아이디, 1이상이면 존재하는 아이디) -->
	<select id="selectCountByUsernick" resultType="int" parameterType="string">
		select count(*) from camp_member where mb_nick=#{mb_nick}
	</select>
	<!-- 8. 이름과 전화번호로 가져오기(아이디찾기 사용) -->
	<select id="selectByUsername" parameterType="hashmap" resultType="MemberVO">
		select * from camp_member where mb_name=#{mb_name} and mb_tel=#{mb_tel}
	</select>
	<!-- 9. ID와과 전화번호로 가져오기(비번찾기 사용) -->
	<select id="selectByUserId" parameterType="hashmap" resultType="MemberVO">
		select * from camp_member where mb_ID=#{mb_ID} and mb_tel=#{mb_tel}
	</select>
	
	<!-- 10. 인증여부를 변경하는 쿼리 -->
	<update id="updateUse" parameterType="hashmap">
		update camp_member set mb_use=#{mb_use} where mb_ID=#{mb_ID} and mb_ID=#{mb_ID}
	</update>
	<!-- 11. 비밀번호 변경하기 -->
	<update id="updatePassword" parameterType="hashmap">
		update camp_member set mb_password=#{mb_password} where mb_ID=#{mb_ID}
	</update>
	
	<!-- 12. ID로 가져오기 -->
	<select id="selectUserId" parameterType="string" resultType="MemberVO">
		select * from camp_member where mb_ID=#{mb_ID}
	</select>
	<!-- 13. 해당이메일의 개수세기(이메일 중복확인 : 0이면 없는 이메일, 1이상이면 존재하는 이메일 -->
	<select id="selectCountByEmail" resultType="int" parameterType="string">
		select count(*) from camp_member where mb_email=#{mb_email}
	</select>
</mapper>